# ─────────────────────────────────────────────
# Netlify configuration — security headers
# ─────────────────────────────────────────────

# Force HTTPS for all traffic
[[redirects]]
  from   = "http://*"
  to     = "https://:splat"
  status = 301
  force  = true

# Security headers applied to every page
[[headers]]
  for = "/*"
  [headers.values]
    # Prevent the site from being embedded in iframes on other domains
    X-Frame-Options = "SAMEORIGIN"

    # Stop browsers from sniffing MIME types
    X-Content-Type-Options = "nosniff"

    # Control how much referrer info is sent when following links
    Referrer-Policy = "strict-origin-when-cross-origin"

    # Disable access to sensitive browser features not needed by this site
    Permissions-Policy = "camera=(), microphone=(), geolocation=(), payment=()"

    # Tell browsers to only connect over HTTPS for the next year
    Strict-Transport-Security = "max-age=31536000; includeSubDomains"

    # Content Security Policy:
    # - Scripts: this site + Netlify Identity widget
    # - Styles: this site + Google Fonts stylesheet
    # - Fonts: Google Fonts CDN only
    # - Frames: Google Maps + YouTube only
    # - Images: this site + data URIs
    # - Everything else: this site only
    Content-Security-Policy = "default-src 'self'; script-src 'self' https://identity.netlify.com 'unsafe-inline'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; frame-src https://www.google.com https://www.youtube.com; img-src 'self' data: blob:; object-src 'none'; base-uri 'self'; form-action 'self' https://api.netlify.com"

# Admin panel — relaxed CSP to allow Decap CMS to load from CDN
[[headers]]
  for = "/admin/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    Content-Security-Policy = "default-src 'self' 'unsafe-inline' 'unsafe-eval' https://unpkg.com https://cdn.jsdelivr.net https://identity.netlify.com https://api.netlify.com https://www.googleapis.com; img-src 'self' data: blob: https:; font-src 'self' https://fonts.gstatic.com https://unpkg.com; frame-src 'self' https://api.netlify.com"
